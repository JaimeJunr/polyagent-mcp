{
  "name": "polyagent-mcp",
  "version": "0.5.0",
  "description": "MCP server that lets any agent delegate to headless Codex, Grok, and Claude Code CLIs — cheap/fast delegation, exploration, surgical reads, filtered command output, and web lookup with model and effort control, with Cursor available as an opt-in fallback.",
  "type": "module",
  "bin": {
    "polyagent-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "prepublishOnly": "npm run build",
    "bench": "npm run build && node bench/aux-bench.mjs",
    "bench:jev": "node bench/jev-bench.mjs"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "cursor",
    "cursor-agent",
    "delegation"
  ],
  "author": "JaimeJunr",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
